#!/bin/bash

filepath="$1"
filepath_without_extension="${filepath%.*}"
mkdir "$filepath_without_extension"_temp
unrar x "$filepath" "$filepath_without_extension"_temp
mv "$filepath_without_extension"_temp "$filepath_without_extension"
