#!/bin/bash

docker stop duplicati
docker rm duplicati
docker run -d --name=duplicati --restart always -v /home/docker/duplicati/config:/config -v /home:/source:ro -e PUID=0 -e PGID=0 -p 8200:8200 linuxserver/duplicati
