#!/usr/bin/env bash

main () {
	declare key_names="$@"
	local key_name
	for key_name in "${key_names}"; do
		local key
		key="${HOME}/.ssh/${key_name}"
		if [[ -f "${key}" ]]; then
			chmod 600 "${key}"
		fi
	done
	eval "$(keychain --eval --quiet $@)"
}

main $@
