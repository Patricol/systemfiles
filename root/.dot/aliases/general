# vi:syntax=sh

# aliases I might use with sudo

# needed for pushing systemfiles
add_ssh_keys() {
        eval "$(keychain --eval --quiet $@)"
}

alias fkill='killall -KILL'
alias vim='svim'

# sudo to other users
alias sup='sudo -i -u patrickcp0'
alias sip='sup'
